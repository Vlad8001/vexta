# Open Graph Images

Drop 1200×630 PNGs here. Referenced from the `<meta property="og:image">` tags across the site.

Expected filenames:
- `og-home.png`, `og-home-ua.png`
- `og-about.png`, `og-about-ua.png`
- `og-services.png`, `og-services-ua.png`
- `og-process.png`, `og-process-ua.png`
- `og-contacts.png`, `og-contacts-ua.png`

**Design suggestion:** VEXTA logo (top-left, ~64px) + page title (centered, Inter 700, 64-72px, white) on a navy→silver gradient background (`linear-gradient(135deg, #0B1D3A 0%, #B8C0CC 100%)`). Add a decorative "V" chevron in the corner.

Once you add the images, no HTML change is needed - the `<meta>` tags already point here.
